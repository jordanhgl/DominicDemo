package com.example.hlint_checks.detectors;

import com.android.tools.lint.detector.api.Detector;
import com.android.tools.lint.detector.api.Detector;

public class GenneralDetector extends Detector implements Detector.UastScanner {
}
